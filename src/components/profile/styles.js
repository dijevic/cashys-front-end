import styled from 'styled-components'
import { theme } from '../../styles/theme'
const { colors } = theme

export const H2 = styled.h2`
font-size:2rem;
line-height:2rem;
color:${colors.white};
margin-top:15px;
font-weight:bold;


`